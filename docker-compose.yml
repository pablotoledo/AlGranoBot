version: '3.8'

services:
  algranobot:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
    restart: always
    volumes:
      - .:/app
    command: python main.py
